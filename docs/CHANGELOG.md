# MusLang PRD — 总变更记录（Changelog）

> **文档**：`docs/prd.md`
> **仓库**：https://github.com/Ink-dark/MusLang
> **范围**：v0.1（2026-08-30） → v0.4.4（当前）

---

## v0.4.4（2026-09-05）—— 网络运行时绑定定稿（D-19）

### 新增章节

| 章节 | 决策编号 | 核心内容 |
|---|---|---|
| §3.15 `net` 事件循环：作为 `std::net` 的依赖自动带入 | **D-19** | **事件循环（event loop / executor）是 `std::net` 的内部依赖**，随 `use std::net` 自动链接；**不提供独立的"运行时选择"**、**不引入 `#[entry]` / `block_on` 注入 API**（对标 Go，P7「网络开箱即用」）；**MVP = 单线程 epoll**（一个 loop 处理全部连接），多线程 / work-stealing = P1（FR-047），io_uring = P1；executor 内部用 **`Box<dyn Future>`**（**D-13 泛型单态化的唯一例外**，避免 `Spawn<F>` 泛型爆炸）；不用 `std::net` 时 event loop 完全不链接，`<8KB` 仍可达（D-11）；嵌入已有 C loop / 内核场景**不使用 `std::net`**、自行基于 `std::sys` + `FfiFuture`（FR-044）实现；**澄清 P5「零运行时」=「零强制运行时」**（可选、不用不链、用了也透明，§3.15.4）；决策表（11 项）+ 用户代码示例（零 boilerplate）+ 与 D-2/D-8/D-13/FR-044/D-12/D-16/D-9 衔接 + 实现期待办 4 项 |

### 同步修改

- §3.7.1 决策表：追加 **D-19**（状态「已定 v0.4.4」），D 编号 D-0 → D-19
- §1.1 说明：待定项移除「async 运行时绑定」，补注「`std::net` 事件循环作为内部依赖（D-19）」
- §12.7（新增）：原「async 运行时绑定」待定项**定稿为 D-19**，归档 A/B/C/D 四方案对比（最终采纳 D「作为 `net` 依赖自动带入」）
- §12.8（新增）：**分配器模型**显式列为待定（锚点：FR-021、FR-008、§3.8.4）
- §15.2 术语表：+6 条（event loop、executor、单线程 epoll、work-stealing、`Box<dyn Future>`、P5「零强制运行时」）
- §15.3：追加 v0.4.4 变更行

### 影响面

- 新增行数：+206（全部在 §3.15 + §12.7 + §12.8 + §15.2/§15.3）
- 其余章节：零改动（仅锚点对齐）
- **决策编号连续性**：D-0 ~ D-19 全部连续、无跳号

---

## v0.4.3（2026-09-05）—— 工程路径定稿（自举 + 包管理 + 软件分发）

### 新增章节

| 章节 | 决策编号 | 核心内容 |
|---|---|---|
| §3.12 自举路径与 staging | D-16 | M1 = 部分自举（前端 MusLang 写 + 后端 Rust 写）；**Stage 0 宿主 = Rust**；先编译 std 再编译编译器；确定性输出/可重现构建**推迟至 M2**；完整自举（含 C99 后端 MusLang 化）**M2/M3** |
| §3.13 包管理器 `mktplace` | D-17 | M1 = **Cargo**（build.rs 驱动 muslangc）；远期 = **`mktplace`**（谐音 **marketplace**）= hypo 分发安全模型 + pets_tools 编排能力合并为单一工具；**三阶段演进 + 量化迁移门槛**（lockfile + 离线 mirror + GPG 签名 + 20 个真实包 + LoongArch64 离线通过） |
| §3.14 软件分发（hypo） | D-18 | **核心澄清**：hypo = 系统级软件分发工具（管产物部署），`mktplace` = 源码级包管理（管依赖/构建），**各司其职**；分工矩阵 + `mktplace.toml` vs `hypo-manifest.toml` 对照 |

### 同步修改

- §3.7.1 决策表：追加 **D-16 / D-17 / D-18**（状态「已定 v0.4.3」），D 编号 D-0 → D-18
- §1.1 说明：把「自举 staging、包管理器」从 §12 待定清单移出
- §3.4 FR-037：自举描述更新为「M1 部分自举」
- §8 阶段三（M3-1~M3-3）：与 §3.12 Stage 0/1/2 对齐（「SHA-256 一致」、「含 C99 后端 MusLang 化」）
- §12.1 comptime：决策节点从「M3-1」精确调整为「**M3-3 自持闭环后**」（与 D-16 时间线一致）
- §12.3 包管理器：重写为跳转 + 历史方案归档（原三方案对比保留为追溯，新设计以 §3.13 / §3.14 为准）
- §15.2 术语表：+6 条（Stage 0 / Stage 1 / Stage 2、`mktplace`、hypo、可重现构建）
- §15.3：追加 v0.4.3 变更行

### 影响面

- 新增行数：+282（全部在 §3.12 / §3.13 / §3.14）
- 其余章节：零改动（仅锚点对齐）

---

## v0.4.2（2026-09-04）—— 语言机制细化（D-13 / D-14 / D-15）

### 新增章节

| 章节 | 决策编号 | 核心内容 |
|---|---|---|
| §3.9 泛型单态化策略 | D-13 | **HIR 层单态化**（后端只见到单态后具体函数）；**递归单态化深度硬限 25 层、超限 = 语法错误**（解析/HIR 构建期拒绝，非警告非链接期）；不引入 `dyn` 类型擦除；配合按需链接 + `--gc-sections` 三层控制膨胀；3 类错误码（`E_MONO_*`） |
| §3.10 生命周期标注策略 | D-14 | **方案 B**：函数签名默认全省略 / 推断失败给建议报错（`E_LIFETIME_AMBIGUOUS` 附建议）；**结构体·枚举不可省**；`'static` 保留；**HRTB 推迟 1.0**；`extern` FFI 边界强制显式标注 |
| §3.11 C++ 互操作边界 | D-15 | **vtable = Itanium C++ ABI**；**异常禁止跨越 FFI 边界**（编译期拒绝 throwing 签名）；**RTTI 不支持**；**模板须 C++ 侧预实例化**（MusLang 只见到具体类型）；1.0 支持边界一览表；与 D-3/D-7/D-4 衔接 |

### 同步修改

- §3.7.1 决策表：追加 D-13 / D-14 / D-15
- §15.3：追加 v0.4.2 变更行

---

## v0.4.1（2026-09-04）—— 语义定稿（D-7 / D-12）

### 新增章节

| 章节 | 决策编号 | 核心内容 |
|---|---|---|
| §3.2.3.1 `defer` 与错误传播 / 异步取消交互 | D-12 | `defer` / `errdefer` 分离（错误路径仅 `errdefer`、LIFO）；`?` 提前返回只跑 `errdefer`；`async fn` 取消仅在 `.await` 点（协作式）；**`defer` 禁止 `.await`**（异步清理须显式 `close().await`）；5 类错误码；附 Zig 官方文档 + Rust Internals + Tokio 引用 |
| §3.8 跨 `.so` 所有权移交协议 | D-7 定稿 | A（严格移交）+ C（标注协议）混合允许、**函数级策略一致**（同函数内禁止 A/C 混用）；B（`Arc` across FFI）为 P1 可选；`MusAllocator::from_c` deallocator 配对；混合生命周期须拆函数；回调走 C；6 项 HIR FFI 校验错误码；Rust FFI 对应关系表 |

### 同步修改

- §3.7.1 决策表：D-7 状态「待冻结」→「已定（v0.4.1）」；追加 D-12
- §15.1：补充权威引用（Zig `defer`/`errdefer`、Rust Internals async Drop、Tokio cancel safety、Huawei Trusted Programming）
- §15.3：追加 v0.4.1 变更行

---

## v0.4（2026-09-04）—— 架构决策落地（D-0 ~ D-12 决策表建立）

### 核心变更

- **定位调整**：改为「类 Rust，**不保证源码级兼容**」（§1.1，D-1）—— FR-001 拆为 001a（视觉相似）+ 001b（明确不兼容）
- **新增 §3.7 架构决策记录**：收录 D-0 ~ D-11 决策表 + 互操作架构（D-3）、共享方言 + 统一 HIR、双 runtime（D-8）、std 三层按需链接 + sys 三端可扩展（D-9/D-10）
- **默认后端**：由 Zig 改为 **C99**（FR-032、§3.4.1、§5.3，D-2）—— 理由：保证极小二进制、避免锁定未 1.0 的 Zig；LLVM = P1；Zig = P2（0.15+ 移除 async/await、`@Frame` 语义漂移）
- **`unsafe` 等价判定**（§3.2.1 K3，D-4）：禁止操作清单 + 固定错误码 + `unsafe_examples/` + `--unsafe-allowed=false` CI 门禁
- **异步模型**（§3.6.1）：改为后端无关实现，通过 `Io` 接口抽象驱动
- **二进制体积口径定稿**（§4.1、§10）：**L1 core 裸启动 < 8KB**（测量协议：`x86_64-linux-musl`、`-Oz -flto`、静态链接、无动态加载）
- **新增 M1-0 决策冻结阶段**（§8）+ v0.4 裁剪建议

### 同步修改

- §3.4.1 编译器架构图更新（C99 为默认后端）
- §5.3 后端映射调整（C99 为主路径）
- §15.3：追加 v0.4 变更行

---

## v0.3（2026-09-01）—— 全面完善

### 新增章节（14 项）

1. §1.4 设计原则（P1 ~ P8）
2. §3.2 类型系统（安全类型 vs 不安全类型）
3. §5 错误处理规范（错误模型 / 错误类型体系 / 后端映射 / `extern` 错误处理）
4. §6 测试策略（测试层级 / 编译器测试基础设施 / CI/CD 要求）
5. §11 风险与缓解（扩展为完整风险表）
6. §12 开放问题分析框架（元编程 / WASM / 包管理器 / 调试信息 / `muslink` 动态链接 / 重定位类型）
7. §13 文档体系（`docs/` 目录结构）
8. §14 治理与版本管理（版本策略 / RFC 流程 / 治理角色）
9. §15.2 术语表

### 补充内容

- §3.4.1 编译器架构图
- §3.4.2 `@cImport` 技术规范
- §3.5.1 `muslink` 技术规范（重定位类型表）
- §3.6.1 异步实现模型 + Zig 新 Io 模型关系
- §4.3 安全模型完善
- §15.1 参考资源（含 Zig / Rust / Go / LLD / ELF / MusKitty 引用）

---

## v0.2（2026-08-30）—— 链接器与 muslink 设计

### 新增章节

- §3.5 链接器（FR-038 ~ FR-042）
- §3.5.1 `muslink` 技术规范（输入/输出格式、支持架构、重定位类型、`--gc-sections`、链接脚本）
- §8 时间线更新（muslink 相关里程碑）

---

## v0.1（2026-08-30）—— 初稿

### 初始内容

- §1 产品概述（一句话定位、核心价值三角融合、目标用户、设计原则雏形）
- §2 用户场景
- §3.1 语法层（FR-001 ~ FR-013）
- §3.3 标准库（FR-014 ~ FR-027）
- §3.3.1 `net::http` API 设计规范
- §4 非功能需求（性能指标、兼容性、安全模型、可用性）
- §7 与 MusCat 生态的集成
- §8 开发阶段与时间线（阶段一/二/三骨架）
- §9 语法示例
- §10 成功指标
- §11 风险与缓解（初版）
- §15 附录（参考资源、术语表雏形、变更记录）

---

## 版本演进速览

| 版本 | 日期 | 行数（估） | 主题 | 决策编号范围 |
|---|---|---|---|---|
| v0.1 | 2026-08-30 | ~300 | 初稿 | — |
| v0.2 | 2026-08-30 | ~450 | 链接器 + muslink | — |
| v0.3 | 2026-09-01 | ~750 | 全面完善（9 大章节） | — |
| v0.4 | 2026-09-04 | ~850 | 架构决策落地 | D-0 ~ D-11 |
| v0.4.1 | 2026-09-04 | ~1100 | 语义定稿（defer + 所有权协议） | D-7（定稿）、D-12 |
| v0.4.2 | 2026-09-04 | ~1300 | 语言机制细化（泛型 / 生命周期 / C++） | D-13、D-14、D-15 |
| v0.4.3 | 2026-09-05 | ~1600 | 工程路径（自举 / 包管理 / 分发） | D-16、D-17、D-18 |
| v0.4.4 | 2026-09-05 | ~1800 | 网络运行时绑定（D-19） | D-19 |

---

## 决策编号索引（D-0 ~ D-19）

| 编号 | 决策 | 定稿版本 | 状态 |
|---|---|---|---|
| D-0 | 共享内存布局规范（IR/HIR） | v0.4 | 待冻结 |
| D-1 | 语法基线（类 Rust，不保证源码兼容） | v0.4 | 已定 |
| D-2 | 编译器后端（MVP C99，LLVM P1，Zig P2） | v0.4 | 已定 |
| D-3 | 互操作机制（共享方言 + 统一 HIR） | v0.4 | 待冻结 |
| D-4 | `unsafe` 等价判定 | v0.4 | P0，待落地 |
| D-5 | 是否引入 MLIR | v0.4 | 待冻结 |
| D-6 | 调用拓扑（同类 runtime 0 开销） | v0.4 | 待冻结 |
| D-7 | 跨 `.so` 所有权移交协议（A+C，函数级一致） | v0.4.1 | 已定 |
| D-8 | 双 runtime（muslang-rt / muslang-rt-c） | v0.4 | 已定 |
| D-9 | 标准库（按需链接 + sys 三端） | v0.4 | 已定 |
| D-10 | 系统接口（rt-c = 唯一系统接口） | v0.4 | 已定 |
| D-11 | L1 范围（no_std 用户态，kernel 远期） | v0.4 | 需确认 |
| D-12 | `defer` 语义（`?` / async 取消） | v0.4.1 | 已定 |
| D-13 | 泛型单态化（HIR 层 + 25 层语法错误） | v0.4.2 | 已定 |
| D-14 | 生命周期标注（方案 B，HRTB 推 1.0） | v0.4.2 | 已定 |
| D-15 | C++ 互操作边界（Itanium ABI + 异常禁跨边界） | v0.4.2 | 已定 |
| D-16 | 自举路径（M1 部分自举 + Rust 宿主） | v0.4.3 | 已定 |
| D-17 | 包管理器（`mktplace`，参考 hypo + pets_tools） | v0.4.3 | 已定 |
| D-18 | 软件分发（hypo 系统级，与 `mktplace` 分工） | v0.4.3 | 已定 |
| D-19 | `net` 运行时绑定（event loop 作为 `std::net` 依赖自动带入，单线程 epoll MVP） | v0.4.4 | 已定 |

---

## 待定项（v0.4.4 时仍未决）

| 章节 | 问题 | 优先级 |
|---|---|---|
| §12.1 | 元编程：`comptime` vs 泛型 + 宏（决策节点：M3-3 后） | P1 |
| §12.2 | WASM 后端 | P1 |
| §12.4 | 调试信息格式（DWARF 5 候选） | P1 |
| §12.8 | `alloc` 分配器零成本抽象（#9）：allocator 参数单态化与 D-13 25 层限制的交互、L1 core 无 libc 默认 allocator、no_std 注入方式 | P0 |

---

> **MusLang — Rust 的壳，Zig 的灵魂，类似 Go 的网络，MusCat 的心脏。**
